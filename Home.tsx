import React, { useState, useMemo, useCallback, FC, memo } from 'react';
import { Text } from 'react-native'

export const CrystalBall: FC = memo(() => {
    return (
        <Text>
            Cletus' Crystal Ball, coming soon...
        </Text>
    )
});
